all:
  hosts:
    localhost:
      ansible_connection: local
  children:
    proxmox_api:
      hosts:
        pve_s1:
          ansible_host: ns3183326.ip-146-59-253.eu
          ansible_connection: local

    db_servers:
      hosts:
        s1_db:
          ansible_host: # TODO: fill in the VM's IP address
          ansible_user: # TODO: fill in SSH user (e.g. root or debian)
          proxmox_vmid: 3037
          proxmox_node: vm002
